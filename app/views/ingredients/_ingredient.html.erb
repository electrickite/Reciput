<li data-id="<%= ingredient.id %>">
  <%= format_amount ingredient.amount(modifier) %>
  <%= ingredient.unit %>
  <%= ingredient.name %><%= ", #{ingredient.notes}" if ingredient.notes.present? %>
  <%= render 'recipes/action_links', model: ingredient %>
</li>
