<%= simple_form_for [@ingredient.recipe, @ingredient], html: {class: 'form-inline resource-form'} do |f| %>
  <%= f.error_notification %>

  <%= f.input_field :recipe_id, as: :hidden %>
  <%= f.input :amount, placeholder: 'Amount', as: :string, label_html: {class: 'sr-only'} %>
  <%= f.input :unit, placeholder: 'Unit', label_html: {class: 'sr-only'} %>
  <%= f.association :food, prompt: 'Food',
    label_html: { class: 'sr-only' },
    input_html: { class: 'selectize-single-create' } %>
  <%= f.input :notes, as: :string, placeholder: 'Notes', label_html: {class: 'sr-only'} %>

  <%= f.button :submit %>
  <%= link_to 'Cancel', :back, class: 'btn btn-link' %>
<% end %>
