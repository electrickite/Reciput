<li>
  <%= format_amount ingredient.amount %>
  <%= ingredient.unit %>
  <%= ingredient.name %><%= ", #{ingredient.notes}" if ingredient.notes.present? %>
  <small><%= ingredient_actions ingredient %></small>
</li>
