<%= simple_form_for [@recipe, @step], html: { class: 'resource-form' } do |f| %>
  <%= f.error_notification %>

  <%= f.input :directions, :input_html => {:rows => 5} %>

  <%= f.button :submit %>
  <%= link_to 'Cancel', :back, class: 'btn btn-link' %>
<% end %>
